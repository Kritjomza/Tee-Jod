import { Clock3,RefreshCw } from "lucide-react";
import type { ParkingLot } from "@/lib/parking/schema";
import { formatBangkokTime } from "@/lib/parking/format";
export function ParkingSummary({bestLot,refreshing,onRefresh}:{bestLot:ParkingLot;refreshing:boolean;onRefresh:()=>void}){ return <section className="hero-card" aria-labelledby="best-option-heading"><div className="hero-top"><div><p className="prototype-label"><span className="pulse"/> Prototype data — not real-time</p><p className="hero-kicker">Best option right now</p><h2 id="best-option-heading">{bestLot.name}</h2></div><button className="refresh-button" onClick={onRefresh} disabled={refreshing} aria-label={refreshing?"Refreshing availability":"Refresh parking availability"}><RefreshCw size={18} className={refreshing?"spin":""} aria-hidden="true"/></button></div><div className="hero-count"><strong>{bestLot.availableSpaces}</strong><span>spaces available</span></div><p className="hero-meta"><Clock3 size={13} aria-hidden="true"/> Last updated {formatBangkokTime(bestLot.updatedAt)}</p></section>; }
